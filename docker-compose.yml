services:
  codia:
    build: .
    ports:
      - "3200:3000"
    restart: always
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_SITE_URL=https://codia.wizycode.fr
    networks:
      - proxy

networks:
  proxy:
    external: true
