---
title: "Les bases du terminal"
description: "Comprendre et utiliser le terminal pour interagir avec ton ordinateur en ligne de commande."
icon: "💻"
level: "debutant"
order: 1
---

Le terminal (aussi appelé console ou ligne de commande) est une interface texte pour interagir avec ton ordinateur. C'est un outil fondamental pour tout développeur.

## Pourquoi apprendre le terminal ?

- **Contrôle total** : Accède à toutes les fonctionnalités de ton système
- **Automatisation** : Exécute des scripts et automatise des tâches répétitives
- **Outils de dev** : La plupart des outils de développement modernes s'utilisent en ligne de commande
- **IA et CLI** : Les outils comme Claude Code fonctionnent dans le terminal

## Ouvrir le terminal

<PlatformContent>
<Mac>

Ouvre **Terminal** (dans Applications → Utilitaires) ou utilise **Spotlight** (Cmd + Espace) et tape "Terminal".

**Pro tip** : Installe [iTerm2](https://iterm2.com/) pour une meilleure expérience.

</Mac>
<Windows>

Ouvre **PowerShell** ou **Windows Terminal** depuis le menu Démarrer.

**Pro tip** : Windows Terminal est plus moderne et supporte plusieurs onglets.

</Windows>
<Linux>

Utilise le raccourci **Ctrl + Alt + T** ou cherche "Terminal" dans tes applications.

Le terminal par défaut dépend de ta distribution (GNOME Terminal, Konsole, etc.).

</Linux>
<WSL>

Ouvre **Windows Terminal** et sélectionne ton profil WSL (Ubuntu, Debian, etc.).

Tu peux aussi taper `wsl` dans PowerShell pour lancer ta distribution par défaut.

</WSL>
</PlatformContent>

## Commandes essentielles

Voici les commandes que tu utiliseras le plus souvent :

### Navigation

```bash
pwd           # Affiche le répertoire courant (Print Working Directory)
ls            # Liste les fichiers et dossiers
cd dossier    # Change de répertoire (Change Directory)
cd ..         # Remonte d'un niveau
cd ~          # Va dans ton répertoire personnel
```

### Manipulation de fichiers

```bash
mkdir nouveau_dossier    # Crée un dossier
touch fichier.txt        # Crée un fichier vide
cp source destination    # Copie un fichier
mv source destination    # Déplace ou renomme
rm fichier               # Supprime un fichier (attention, pas de corbeille !)
```

### Affichage

```bash
cat fichier.txt     # Affiche le contenu d'un fichier
less fichier.txt    # Affiche avec pagination (q pour quitter)
head -n 10 fichier  # Affiche les 10 premières lignes
tail -n 10 fichier  # Affiche les 10 dernières lignes
```

## Raccourcis clavier utiles

| Raccourci | Action |
|-----------|--------|
| `Tab` | Auto-complétion |
| `Ctrl + C` | Annule la commande en cours |
| `Ctrl + L` | Efface l'écran |
| `↑` / `↓` | Navigue dans l'historique |
| `Ctrl + R` | Recherche dans l'historique |

## Bonnes pratiques

1. **Utilise Tab** pour auto-compléter les noms de fichiers et éviter les erreurs
2. **Lis avant d'exécuter** : Vérifie toujours tes commandes, surtout avec `rm`
3. **Fais des backups** avant d'exécuter des commandes destructrices
4. **Apprends progressivement** : Pas besoin de tout mémoriser d'un coup

## Prochaines étapes

Une fois à l'aise avec ces bases, tu pourras :
- Installer des outils de développement
- Utiliser Git pour le versioning
- Exécuter des scripts et automatiser des tâches
- Utiliser des outils IA en ligne de commande comme Claude Code
