---
title: "Git et le versioning"
description: "Les bases de Git pour suivre l'historique de ton code et collaborer efficacement."
icon: "📝"
level: "debutant"
order: 4
---

Git est un système de contrôle de version qui te permet de suivre les modifications de ton code, de collaborer avec d'autres développeurs et de revenir en arrière si nécessaire.

## Pourquoi Git ?

- **Historique** : Chaque modification est enregistrée
- **Collaboration** : Plusieurs personnes peuvent travailler sur le même projet
- **Branches** : Travaille sur des fonctionnalités isolées
- **Sauvegarde** : Ton code est sauvegardé sur des serveurs distants

## Installation

<PlatformContent>
<Mac>

```bash
# Avec Homebrew (recommandé)
brew install git

# Vérifie l'installation
git --version
```

</Mac>
<Windows>

```powershell
# Avec winget
winget install Git.Git

# Ou télécharge depuis https://git-scm.com/download/win

# Vérifie l'installation
git --version
```

</Windows>
<Linux>

```bash
# Ubuntu/Debian
sudo apt update && sudo apt install git

# Fedora
sudo dnf install git

# Vérifie l'installation
git --version
```

</Linux>
<WSL>

```bash
# Dans WSL (Ubuntu)
sudo apt update && sudo apt install git

# Vérifie l'installation
git --version
```

</WSL>
</PlatformContent>

## Configuration initiale

Après l'installation, configure ton identité :

```bash
git config --global user.name "Ton Nom"
git config --global user.email "ton@email.com"

# Optionnel : définir l'éditeur par défaut
git config --global core.editor "code --wait"  # VS Code
```

## Concepts fondamentaux

### Le repository (dépôt)

Un **repository** est un dossier dont Git suit les modifications.

```bash
# Créer un nouveau repo
mkdir mon-projet && cd mon-projet
git init

# Cloner un repo existant
git clone https://github.com/user/repo.git
```

### Les trois zones

Git organise ton travail en trois zones :

```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│  Working Dir    │────▶│   Staging Area  │────▶│   Repository    │
│  (Tes fichiers) │     │   (git add)     │     │   (git commit)  │
└─────────────────┘     └─────────────────┘     └─────────────────┘
```

1. **Working Directory** : Tes fichiers actuels
2. **Staging Area** : Changements prêts à être commités
3. **Repository** : Historique des commits

## Commandes essentielles

### Voir l'état actuel

```bash
git status              # État des fichiers
git diff                # Changements non stagés
git diff --staged       # Changements stagés
git log --oneline       # Historique compact
```

### Workflow de base

```bash
# 1. Modifier des fichiers...

# 2. Voir ce qui a changé
git status

# 3. Ajouter les fichiers au staging
git add fichier.js           # Un fichier spécifique
git add .                    # Tous les fichiers modifiés

# 4. Créer un commit
git commit -m "Ajoute la fonction de login"

# 5. Envoyer vers le serveur distant
git push origin main
```

### Récupérer des changements

```bash
git fetch               # Récupère les infos du serveur (sans modifier)
git pull origin main    # Récupère ET fusionne les changements
```

## Les branches

Les branches te permettent de travailler sur des fonctionnalités isolées.

```bash
# Créer et basculer sur une nouvelle branche
git checkout -b feature/login

# Lister les branches
git branch

# Changer de branche
git checkout main

# Fusionner une branche dans main
git checkout main
git merge feature/login

# Supprimer une branche
git branch -d feature/login
```

### Workflow recommandé

```
main ─────────●──────────●──────────●───────▶
               \        /
feature ────────●──────●
```

1. Crée une branche pour ta fonctionnalité
2. Fais tes commits sur cette branche
3. Fusionne (merge) dans main quand c'est prêt

## Bonnes pratiques

### Messages de commit

Un bon message de commit est :
- **Court** : 50 caractères max pour la première ligne
- **Descriptif** : Explique le "quoi" et le "pourquoi"
- **Au présent** : "Ajoute" plutôt que "Ajouté"

```bash
# Mauvais
git commit -m "fix"
git commit -m "changes"

# Bon
git commit -m "Corrige le bug de validation email"
git commit -m "Ajoute la pagination sur la liste des utilisateurs"
```

### Commits atomiques

Chaque commit devrait représenter **une seule modification logique** :

```bash
# Un seul commit avec tout - Mauvais
git commit -m "Ajoute login, corrige bug header, met à jour README"

# Plusieurs commits atomiques - Bon
git commit -m "Ajoute le système d'authentification"
git commit -m "Corrige l'alignement du header"
git commit -m "Met à jour la documentation d'installation"
```

## Résoudre les problèmes courants

### Annuler des changements

```bash
# Annuler les modifications d'un fichier (non stagé)
git checkout -- fichier.js

# Retirer du staging (sans perdre les modifications)
git reset HEAD fichier.js

# Annuler le dernier commit (garde les fichiers)
git reset --soft HEAD~1
```

### Conflits de merge

Quand Git ne peut pas fusionner automatiquement :

```bash
# 1. Git indique les fichiers en conflit
git status

# 2. Ouvre les fichiers et résous les conflits
# Cherche les marqueurs <<<<<<< ======= >>>>>>>

# 3. Ajoute les fichiers résolus
git add fichier-resolu.js

# 4. Termine le merge
git commit
```

## Prochaines étapes

Une fois à l'aise avec les bases :
- Explore les **pull requests** sur GitHub/GitLab
- Apprends le **rebase** pour un historique propre
- Configure des **hooks** pour automatiser des vérifications
